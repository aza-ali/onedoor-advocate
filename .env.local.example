# The brain. Server-only. Never shipped to the browser.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-opus-4-8
# Voice I/O only (Gemini speech-to-text + verbatim text-to-speech). Server-only. Optional; the text chat works without it.
GEMINI_API_KEY=
# optional voice overrides:
# GEMINI_TTS_VOICE=Kore
